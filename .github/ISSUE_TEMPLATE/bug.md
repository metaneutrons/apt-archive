---
name: Bug
about: A fault in the archive or in the pipeline
labels: bug
---

## What happens

## What was expected

## Environment

Distribution and version, `apt --version`, the verifier in use (`gpgv` or
`sqv`), the archive domain and prefix affected.

## Output

<!-- The complete output of `apt-get update`, not shortened. The key ID in a
     NO_PUBKEY or missing-key message names the signing subkey, not the primary
     key. -->
